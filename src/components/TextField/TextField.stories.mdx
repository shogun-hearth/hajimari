import { useState } from 'react';
import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { withDesign } from 'storybook-addon-designs';
import { Box } from '@mui/material';
import AllInclusiveIcon from '@mui/icons-material/AllInclusive';

import TextField, { TextFieldProps } from './TextField';
import StatefulTextField from './StatefulTextField';
import Typography from '../Typography';

export const argTypes = {
  width: {
    control: { type: 'number' },
    defaultValue: '100%',
  },
  mask: {
    control: { type: 'select' },
    options: ['money', 'moneyWithCents', 'ssn', 'lastFourSsn', 'bankNumber', 'phone', 'percent', 'taxId', 'year', 'default', 'search'],
    defaultValue: 'default',
  },
};

<Meta
  title="Molecules/TextField"
  component={TextField}
  argTypes={argTypes}
  parameters={{
    actions: { disabled: true },
    layout: 'padded',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/MsUWgABtjnh8LAttdex2q1/Quotes?node-id=571%3A1104',
    },
  }}
/>

export const TextFieldTemplate = (args) => <StatefulTextField {...args} />;

# TextField

**DATE ADDED**: 12/03/21 | **PROJECT**: [Quotes](https://www.notion.so/gethearth/Quotes-Bid-Estimate-Tool-c05b0b74bbb9404f8e2cc40dfdec044a)

A `TextField` is essentially two `string` values with dots as a separator between them.
First added to support a receipt-esque run-down of values and totals in a `Quote`, with multiple line items that
add up to some total value.

<br />

---

<br />

### Usage and examples

It has props `leftContent` and `rightContent`, which are both of type `ReactNode`. It also has a
`width` prop, which is `100%` by default, and accepts a `number` or `string` so that the width can be
a percentage or absolute pixel value.

<br />

#### Default / Basic:

> The most basic usage of a `TextField`. The default `Typography` variant used is `p1`.
<!-- 
<Canvas>
  <Story
    name="Basic"
    component={TextField}
    args={{ leftContent: 'Discount', rightContent: '-$10.00' }}
  >
    {TextFieldTemplate.bind()}
  </Story>
</Canvas> -->

#### Typography variant

> Since `leftContent` and `rightContent` both accept `ReactNode`s, we can pass in a different
> `Typography` element to render other variants or colors.
<!-- 
<Canvas>
  <Story
    name="Typography variant"
    args={{
      leftContent: (
        <Typography variant="h2" weight="semibold">
          Total
        </Typography>
      ),
      rightContent: (
        <Typography variant="h2" weight="semibold" color="success500">
          1,714.0
        </Typography>
      ),
    }}
  >
    {TextFieldTemplate.bind()}
  </Story>
</Canvas> -->

#### Component variant

> We can also pass in any other component to`leftContent` and `rightContent`, such as an `icon`.

<Canvas>
  <Story
    name="Component variant"
    args={{
    }}
  >
    {TextFieldTemplate.bind()}
  </Story>
</Canvas>

---

<br />

### Playground

<Canvas>
  <Story name="Playground">{TextFieldTemplate.bind()}</Story>
</Canvas>

<ArgsTable of={TextField} story="Playground" />
