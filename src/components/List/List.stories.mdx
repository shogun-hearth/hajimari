import { Meta } from "@storybook/addon-docs";
import { withDesign } from 'storybook-addon-designs';
import { IconButton, Switch } from '@mui/material';
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';

import Button from '../Button';
import ListItem from '../ListItem';
import Box from '../Box';

<Meta
  title="Molecules/List"
  component={List}
/>

export const argTypes = {
  children: {
    control : {
      type: 'select',
    }
  }
}

export const ListTemplate = (args) => {
return (
  <Box width='900px'>
    <List {...args}>
      <ListItem
        headerText="Payment Options On Invoices"
        description="Show or hide payment options for all of your customers"
        divider
        header
      >
        <Button color="primary" variant="primary" onClick={() => {}}>
          Edit Account
        </Button>
      </ListItem>
      <ListItem
        headerText="Accept Credit/ Debit Card Payments"
        description="Credit/Debit Card Payment fee is 3.0% + $0.00 per payment."
        divider
      >
        <IconButton>
          <ExpandMoreIcon />
        </IconButton>
      </ListItem>
      <ListItem
        headerText="Accept Credit/ Debit Card Payments"
        description="Credit/Debit Card Payment fee is 3.0% + $0.00 per payment."
        header
      >
        <Switch color="primary" />
      </ListItem>
    </List>
  </Box>
  )
}

# TODO: Component name

**DATE ADDED**: [Date] | **PROJECT**: [Project name with link to Notion, Figma, etc]

**TODO** -- add a "user facing" description of the component here; include why and where it was first added,
the general use case, and any other relevant context.

<br />

---

<br />

### Usage and examples

**TODO** -- add a more technical description of the component here, which will generally be some information
about the available props; this can be listed as a table:

<br />

#### Default / Basic:

> Short description of the basic use case of this component. Include a code snippet and canvas.

<Canvas>
  <Story name="Basic" component={List} args={{}}>{ListTemplate.bind({})}</Story>
</Canvas>

#### Variants

**TODO** -- add other variants / usage examples here, following the same format as the "Basic" example.

<br />

---

<br />

### Playground

**TODO** -- This is where the component will actually be rendered with [interactive controls](https://storybook.js.org/docs/react/essentials/controls/). There should be a control
for each prop that someone can change (i.e. background color, border color, width, or whatever else is appropriate for a given
component).

<Canvas>
  <Story name="Playground">{ListTemplate.bind()}</Story>
</Canvas>
