import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import DatePickerField from "./DatePickerField";

export const argTypes = {
  label: {
    control: { type: 'text' },
    defaultValue: 'Label',
  },
  disabled: {
    control: { type: 'boolean' },
    defaultValue: false,
  },
  error: {
    control: { type: 'text' },
  },
  helperText: {
    control: { type: 'text' },
  },
};

<Meta
  title="Atoms/Field/DatePickerField"
  argTypes={argTypes}
  component={DatePickerField}
/> 

export const DatePickerTemplate = (args) => <DatePickerField {...args} />;

## DatePickerField

A wrapper around the base `DatePicker` component from the MUI component set.
Makes use of our custom `TextField` component instead of the base MUI `TextField`.

Can be consumed both in Formik and not-Formik forms.

<br />

---

<br />

### Playground

<Canvas>
  <Story name="Sandbox">
    {DatePickerTemplate.bind()}
  </Story>
</Canvas>

<ArgsTable of={DatePickerTemplate} story="Playground" />