import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import SelectField from './SelectField';

<Meta title='Atoms/Field/SelectField' component={SelectField} argTypes={{}} />

export const SelectFieldTemplate = (args) => <SelectField {...args} />;

# SelectField

#### Default / Basic:

<Canvas>
  <Story
    name='Basic'
    component={SelectField}
    args={{
      options: [
        { value: 'one', label: 'one' },
        { value: 'two', label: 'two' },
      ],
      label: 'Label',
    }}
  >
    {SelectFieldTemplate.bind()}
  </Story>
</Canvas>
