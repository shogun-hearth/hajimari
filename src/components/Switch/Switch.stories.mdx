import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Switch from './Switch.tsx';

<Meta 
  title="Molecules/Switch"
  component={Switch}
  argTypes={{}}
  parameters={{
    actions: { disabled: true }
  }}
/>

export const SwitchTemplate = (args) => <Switch {...args} />;

# Switch


#### Default / Basic:

> The most basic usage of a `Switch`.

<Canvas>
  <Story
    name="Basic"
    component={Switch}
    args={{}}
  >
    {SwitchTemplate.bind()}
  </Story>
</Canvas>


# Switch

Buttons help people take action. Choose the type of button based on the importance of the action.
The more important the action is, the more emphasis its button should have.
Button labels use **title case**.

## States

Switch have a _default_, _hover_, or _disabled_ state. The hover state is generally a darkened
hue of the `color` of the button.

