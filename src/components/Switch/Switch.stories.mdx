import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Switch from './Switch.tsx';

<Meta 
  title="Molecules/Switch"
  component={Switch}
  parameters={{
    actions: { disabled: true }
  }}
/>

export const SwitchTemplate = (args) => <Switch {...args} />;

# Switch


#### Default / Basic:

> The most basic usage of a `Switch`.

<Canvas>
  <Story
    name="Basic"
    component={Switch}
  >
    {SwitchTemplate.bind()}
  </Story>
</Canvas>


# Switch

Use switches to toggle a single item, immediatly activate or deactivate something.

## States

Switches have a _disabled_, or _selected_ state. 


